name: Markdown Lint
on:
  pull_request:
    paths:
      - '**.md'

jobs:
  markdown-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint Markdown
        uses: actionshub/markdownlint@v3.1.1
        with:
          files_to_lint: '**/*.md'